<h1>新規投稿</h1>
<%= form_with model: @post, local: true do |form| %>
  <div>
    <%= form.text_field :body, required: true %>
  </div>
  <div>
    <%= form.submit "送信" %>
  </div>
<% end %>
<h1>投稿一覧</h1>
<%= render partial: "form", collection: @posts %>
<%= paginate @posts %>